<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="300dp"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/text_receiver_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="@string/label_info_target_account"
        android:textSize="16sp"
        android:textAlignment="center"
        android:layout_marginTop="20dp"/>

    <EditText
        android:id="@+id/edit_amount"
        android:layout_width="166dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_receiver_info"
        android:layout_alignStart="@id/edit_new_description"
        android:layout_marginTop="20dp"
        android:hint="@string/label_amount"
        android:inputType="numberDecimal"
        android:autofillHints="amount" />

    <TextView
        android:id="@+id/text_currency"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/edit_amount"
        android:layout_toEndOf="@id/edit_amount"
        android:text="@string/label_currency"
        android:textSize="24sp" />

    <EditText
        android:id="@+id/edit_new_description"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_amount"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        android:hint="@string/label_description"
        android:autofillHints="description" />
</RelativeLayout>